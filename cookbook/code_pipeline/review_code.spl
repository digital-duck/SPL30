-- review_code.spl
-- Sub-workflow: review code and return structured feedback.

WORKFLOW review_code
    INPUT:  @code TEXT
    OUTPUT: @feedback TEXT
DO
    SELECT @code AS source
    GENERATE reviewer(@code) INTO @feedback
    LOGGING 'review_code: feedback generated'
    COMMIT @feedback
END
