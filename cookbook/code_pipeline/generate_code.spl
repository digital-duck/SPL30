-- generate_code.spl
-- Sub-workflow: generate Python code from a natural-language specification.

WORKFLOW generate_code
    INPUT:  @spec TEXT
    OUTPUT: @code TEXT
DO
    SELECT @spec AS task
    GENERATE coder(@spec) INTO @code
    LOGGING INFO 'generate_code: produced ' || @code
    COMMIT @code
END
