
```bash
spl run ./05_self_refine/self_refine.spl --adapter momagrid task=Write a haiku about coding
```

```spl
-- SPL 2.0: Self-Refine Pattern
-- Iteratively improves output through critique and refinement

WORKFLOW self_refine
  INPUT:
    @task TEXT,
    @max_iterations INTEGER DEFAULT 5,
    @log_dir TEXT DEFAULT 'cookbook/05_self_refine/logs'
  OUTPUT: @result TEXT
DO
  @iteration := 0

  LOGGING f'Self-refine started | max_iterations={@max_iterations}' LEVEL INFO

  -- Initial draft
  GENERATE draft(@task) INTO @current
  LOGGING 'Initial draft ready' LEVEL INFO
  CALL write_file(f'{@log_dir}/draft_0.md', @current) INTO @_

  -- Iterative refinement loop
  WHILE @iteration < @max_iterations DO
    LOGGING f'Iteration {@iteration} | critiquing ...' LEVEL DEBUG
    GENERATE critique(@current) INTO @feedback
    CALL write_file(f'{@log_dir}/feedback_{@iteration}.md', @feedback) INTO @_

    EVALUATE @feedback
      WHEN 'satisfactory' THEN
        LOGGING f'Satisfactory at iteration {@iteration}' LEVEL INFO
        CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
        COMMIT @current WITH status = 'complete', iterations = @iteration
      ELSE
        GENERATE refined(@current, @feedback) INTO @current
        @iteration := @iteration + 1
        CALL write_file(f'{@log_dir}/draft_{@iteration}.md', @current) INTO @_
        LOGGING f'Refined | iteration={@iteration}' LEVEL DEBUG
    END
  END

  -- If loop exhausted, commit best effort
  LOGGING f'Max iterations reached | iterations={@iteration}' LEVEL WARN
  CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
  COMMIT @current WITH status = 'max_iterations', iterations = @iteration

EXCEPTION
  WHEN MaxIterationsReached THEN
    CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
    COMMIT @current WITH status = 'partial'
  WHEN BudgetExceeded THEN
    COMMIT @current WITH status = 'budget_limit'
END
```

[INFO] Self-refine started | max_iterations=5
[INFO] Initial draft ready
[INFO] Satisfactory at iteration 2
============================================================
Caller:  momagrid-hub
Adapter: momagrid
Status: complete
LLM Calls: 9
Tokens: 3213 in / 1813 out
Latency: 112773ms
Timestamp: 2026-03-29 22:16:12
------------------------------------------------------------
```output
Thank you for providing a detailed analysis of my poem. The feedback is incredibly helpful in refining my work.

I appreciate the observation that the tone of the poem is clarified, but I agree that adding more emotional resonance would make it even stronger. To achieve this, I will consider incorporating sensory details and personal connections to make the reader invest in the issue with the code. Perhaps I can add lines that describe the smell of burning circuitry or the feeling of frustration when trying to debug a particular error.

Regarding the phrase "Where silence screams," you're right that it might be helpful to provide more context about what kind of silence is screaming. I can explore this idea further by introducing specific details, such as the sound of the computer's fans still running, even though the system has been shut down for hours.

I also appreciate your suggestion to incorporate subtle details that suggest the mystery is related to a particular problem or issue with the code. This could be achieved by mentioning specific error messages, system crashes, or symptoms. For example, I could add a line that describes a "log file full of cryptic warnings" or a "screen displaying an endless loop of errors."

Finally, I'll consider adding more language to help the reader visualize what these puzzles are and how they contribute to the overall decay of the codebase. Perhaps I can describe the "puzzles" as tangled threads of code that seem impossible to untangle.

With this feedback in mind, here's a revised version of my poem:

Lines of code descend like fallen leaves
Each line a puzzle, shrouded in darkness and pain
A log file full of cryptic warnings lies in wait
Error messages echo through the void, "There's something wrong with your design"

The fans still run, though the system's down
A chilling reminder that the problem's still around
In this codebase crumbling, where silence screams
I'm left to debug, in a sea of digital dreams

In this revised version, I've added more sensory details and context to help the reader understand the issue with the code. I hope this version better captures the emotional resonance you suggested and provides a stronger connection between the poem's themes.

Again, thank you for your insightful feedback. It has been incredibly helpful in refining my work and pushing me to explore new ideas and perspectives.
```
------------------------------------------------------------
Variables:
  @task = Write a haiku about coding
  @max_iterations = 5
  @log_dir = cookbook/05_self_refine/logs
  @iteration = 2
  @_ = written: cookbook/05_self_refine/logs/final.md
  @feedback = The provided input is actually a critique of an analysis or feedback, not a piece of writing itself. However, I can analyze it from the perspective of the original poem's author.

Strengths:

1. The poet shows appreciation for the critic's feedback and acknowledges areas for improvement.
2. They demonstrate a willingness to incorporate suggestions and expand on their ideas.
3. The revised version of the poem addresses some of the critic's concerns, such as adding sensory details and context.

Weaknesses:

1. Some of the language and imagery may still feel a bit generic or cliche (e.g., "lines of code descend like fallen leaves," "puzzles shrouded in darkness and pain").
2. The poet could further explore the emotional resonance by delving deeper into their personal connections to the issue with the code.
3. While the revised version addresses some of the critic's concerns, it may still benefit from more specific and detailed descriptions of the codebase's decay.

Suggestions for improvement:

1. Consider adding more nuance and specificity to your language and imagery to make the poem stand out.
2. Think about how you can further tap into your personal connections to make the emotional resonance more impactful.
3. Experiment with different sentence structures and word choices to create a more dynamic and engaging reading experience.

Overall, the poet has demonstrated a willingness to learn and improve based on feedback, which is essential for refinement and growth as a writer.
============================================================
Log: /home/hpgong/.spl/logs/self_refine-momagrid-20260329-221419.md
