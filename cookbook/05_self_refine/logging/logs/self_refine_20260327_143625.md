
```bash
spl run ./05_self_refine/self_refine.spl --adapter momagrid task=Write a haiku about coding
```

```spl
-- SPL 2.0: Self-Refine Pattern
-- Iteratively improves output through critique and refinement

WORKFLOW self_refine
  INPUT:
    @task TEXT,
    @max_iterations INTEGER DEFAULT 5,
    @log_dir TEXT DEFAULT 'cookbook/05_self_refine/logs'
  OUTPUT: @result TEXT
DO
  @iteration := 0

  LOGGING f'Self-refine started | max_iterations={@max_iterations}' LEVEL INFO

  -- Initial draft
  GENERATE draft(@task) INTO @current
  LOGGING 'Initial draft ready' LEVEL INFO
  CALL write_file(f'{@log_dir}/draft_0.md', @current) INTO @_

  -- Iterative refinement loop
  WHILE @iteration < @max_iterations DO
    LOGGING f'Iteration {@iteration} | critiquing ...' LEVEL DEBUG
    GENERATE critique(@current) INTO @feedback
    CALL write_file(f'{@log_dir}/feedback_{@iteration}.md', @feedback) INTO @_

    EVALUATE @feedback
      WHEN 'satisfactory' THEN
        LOGGING f'Satisfactory at iteration {@iteration}' LEVEL INFO
        CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
        COMMIT @current WITH status = 'complete', iterations = @iteration
      ELSE
        GENERATE refined(@current, @feedback) INTO @current
        @iteration := @iteration + 1
        CALL write_file(f'{@log_dir}/draft_{@iteration}.md', @current) INTO @_
        LOGGING f'Refined | iteration={@iteration}' LEVEL DEBUG
    END
  END

  -- If loop exhausted, commit best effort
  LOGGING f'Max iterations reached | iterations={@iteration}' LEVEL WARN
  CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
  COMMIT @current WITH status = 'max_iterations', iterations = @iteration

EXCEPTION
  WHEN MaxIterationsReached THEN
    CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
    COMMIT @current WITH status = 'partial'
  WHEN BudgetExceeded THEN
    COMMIT @current WITH status = 'budget_limit'
END
```

[INFO] Self-refine started | max_iterations=5
[INFO] Initial draft ready
[INFO] Satisfactory at iteration 0
============================================================
Caller:  momagrid-hub
Adapter: momagrid
Status: complete
LLM Calls: 3
Tokens: 415 in / 293 out
Latency: 18465ms
Timestamp: 2026-03-27 14:36:44
------------------------------------------------------------
```output
Lines of code dance bright
Algorithms whispered sweet
Logic's gentle song
```
------------------------------------------------------------
Variables:
  @task = Write a haiku about coding
  @max_iterations = 5
  @log_dir = cookbook/05_self_refine/logs
  @iteration = 0
  @_ = written: cookbook/05_self_refine/logs/final.md
  @feedback = This is a poetic piece, not a program in the classical sense. However, if we interpret it as a creative expression or a piece of writing, here's my critique:

* The use of metaphor ("lines of code dance bright") and personification ("algorithms whispered sweet") creates vivid imagery and adds depth to the poem.
* The comparison between "algorithms" and a gentle song is particularly effective in conveying a sense of calm and orderliness, which is often associated with coding.
* However, some readers might find the language and tone too simplistic or even childish (e.g., "Logic's gentle song"). Consider using more complex vocabulary or nuanced expressions to convey the subject matter.
* The poem seems to celebrate the beauty and elegance of code. While this can be a compelling message, it may come across as overly positive or idealized. Consider exploring potential drawbacks or complexities associated with coding.
* Lastly, the structure and rhythm of the poem are well-executed. It has a clear, consistent pattern that adds to its musicality.

Some possible suggestions for refinement:

* Experiment with more diverse and evocative language to create a richer atmosphere.
* Explore subtle variations in tone and emotion to add depth and complexity to the poem.
* Consider adding specific details or references that ground the poetry in real-world coding experiences, making it more relatable and engaging.
============================================================
Log: /home/papagame/.spl/logs/self_refine-momagrid-20260327-143625.md
