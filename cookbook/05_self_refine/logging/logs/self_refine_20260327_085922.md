
```bash
spl run ./05_self_refine/self_refine.spl --adapter momagrid task=Write a haiku about coding
```

```spl
-- SPL 2.0: Self-Refine Pattern
-- Iteratively improves output through critique and refinement

WORKFLOW self_refine
  INPUT:
    @task TEXT,
    @max_iterations INTEGER DEFAULT 5,
    @log_dir TEXT DEFAULT 'cookbook/05_self_refine/logs'
  OUTPUT: @result TEXT
DO
  @iteration := 0

  LOGGING f'Self-refine started | max_iterations={@max_iterations}' LEVEL INFO

  -- Initial draft
  GENERATE draft(@task) INTO @current
  LOGGING 'Initial draft ready' LEVEL INFO
  CALL write_file(f'{@log_dir}/draft_0.md', @current) INTO @_

  -- Iterative refinement loop
  WHILE @iteration < @max_iterations DO
    LOGGING f'Iteration {@iteration} | critiquing ...' LEVEL DEBUG
    GENERATE critique(@current) INTO @feedback
    CALL write_file(f'{@log_dir}/feedback_{@iteration}.md', @feedback) INTO @_

    EVALUATE @feedback
      WHEN 'satisfactory' THEN
        LOGGING f'Satisfactory at iteration {@iteration}' LEVEL INFO
        CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
        COMMIT @current WITH status = 'complete', iterations = @iteration
      ELSE
        GENERATE refined(@current, @feedback) INTO @current
        @iteration := @iteration + 1
        CALL write_file(f'{@log_dir}/draft_{@iteration}.md', @current) INTO @_
        LOGGING f'Refined | iteration={@iteration}' LEVEL DEBUG
    END
  END

  -- If loop exhausted, commit best effort
  LOGGING f'Max iterations reached | iterations={@iteration}' LEVEL WARN
  CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
  COMMIT @current WITH status = 'max_iterations', iterations = @iteration

EXCEPTION
  WHEN MaxIterationsReached THEN
    CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
    COMMIT @current WITH status = 'partial'
  WHEN BudgetExceeded THEN
    COMMIT @current WITH status = 'budget_limit'
END
```

[INFO] Self-refine started | max_iterations=5
[INFO] Initial draft ready
[INFO] Satisfactory at iteration 0
============================================================
Caller:  momagrid-hub
Adapter: momagrid
Status: complete
LLM Calls: 3
Tokens: 480 in / 358 out
Latency: 10316ms
Timestamp: 2026-03-27 08:59:33
------------------------------------------------------------
```output
Lines of code descend
Logic's gentle, guiding hand
Silicon's soft song
```
------------------------------------------------------------
Variables:
  @task = Write a haiku about coding
  @max_iterations = 5
  @log_dir = cookbook/05_self_refine/logs
  @iteration = 0
  @_ = written: cookbook/05_self_refine/logs/final.md
  @feedback = A poetic critique!

Here are some thoughts on the input:

* The language is simple and evocative, which suits the theme of technology. The use of "descend" to describe lines of code creates a sense of movement and progression.
* The phrase "Logic's gentle, guiding hand" is particularly effective in conveying the idea that logic is a guiding force behind programming decisions. It also has a soothing quality to it, which contrasts nicely with the often-chaotic nature of coding.
* The final line, "Silicon's soft song," is a bit more enigmatic and might be open to interpretation. While silicon is a key material in electronics, describing it as having a "soft song" is unusual. However, it could suggest a gentle, melodic quality that belies the complexity of digital systems.

If I were to offer some constructive criticism, I might suggest:

* Consider adding more sensory details to enhance the poem's overall impact. For example, what does the "silicon's soft song" sound like? Is it a hum, a whir, or something else?
* You might want to explore the contrast between the "gentle hand" of logic and the potential frustrations of coding. This could add depth and nuance to the poem.
* Finally, you might consider varying your sentence structure and line lengths to create more visual interest on the page. While the current lines have a simple, flowing quality, some variation in terms (e.g., shorter sentences, caesuras) could help to build tension or emphasize certain words.

Overall, however, this is a well-crafted poem that effectively conveys the relationship between technology and creativity!
============================================================
Log: /home/papagame/.spl/logs/self_refine-momagrid-20260327-085922.md
