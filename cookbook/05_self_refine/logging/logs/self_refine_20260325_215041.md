
```bash
spl run ./05_self_refine/self_refine.spl --adapter ollama -m gemma3 task=Write a haiku about coding
```

```spl
-- SPL 2.0: Self-Refine Pattern
-- Iteratively improves output through critique and refinement

WORKFLOW self_refine
  INPUT:
    @task TEXT,
    @max_iterations INTEGER DEFAULT 5,
    @log_dir TEXT DEFAULT 'cookbook/05_self_refine/logs'
  OUTPUT: @result TEXT
DO
  @iteration := 0

  LOGGING f'Self-refine started | max_iterations={@max_iterations}' LEVEL INFO

  -- Initial draft
  GENERATE draft(@task) INTO @current
  LOGGING 'Initial draft ready' LEVEL INFO
  CALL write_file(f'{@log_dir}/draft_0.md', @current) INTO @_

  -- Iterative refinement loop
  WHILE @iteration < @max_iterations DO
    LOGGING f'Iteration {@iteration} | critiquing ...' LEVEL DEBUG
    GENERATE critique(@current) INTO @feedback
    CALL write_file(f'{@log_dir}/feedback_{@iteration}.md', @feedback) INTO @_

    EVALUATE @feedback
      WHEN 'satisfactory' THEN
        LOGGING f'Satisfactory at iteration {@iteration}' LEVEL INFO
        CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
        COMMIT @current WITH status = 'complete', iterations = @iteration
      ELSE
        GENERATE refined(@current, @feedback) INTO @current
        @iteration := @iteration + 1
        CALL write_file(f'{@log_dir}/draft_{@iteration}.md', @current) INTO @_
        LOGGING f'Refined | iteration={@iteration}' LEVEL DEBUG
    END
  END

  -- If loop exhausted, commit best effort
  LOGGING f'Max iterations reached | iterations={@iteration}' LEVEL WARN
  CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
  COMMIT @current WITH status = 'max_iterations', iterations = @iteration

EXCEPTION
  WHEN MaxIterationsReached THEN
    CALL write_file(f'{@log_dir}/final.md', @current) INTO @_
    COMMIT @current WITH status = 'partial'
  WHEN BudgetExceeded THEN
    COMMIT @current WITH status = 'budget_limit'
END
```

[INFO] Self-refine started | max_iterations=5
[INFO] Initial draft ready
[INFO] Satisfactory at iteration 0
============================================================
Status: complete
LLM Calls: 3
Tokens: 489 in / 367 out
Latency: 6143ms
------------------------------------------------------------
```output
Lines of code descend
Logic's gentle, steady hum
Mind in digital
```
------------------------------------------------------------
Variables:
  @task = Write a haiku about coding
  @max_iterations = 5
  @log_dir = cookbook/05_self_refine/logs
  @iteration = 0
  @_ = written: cookbook/05_self_refine/logs/final.md
  @feedback = A poetic and introspective piece!

Here's a critique:

Strengths:

* The language is simple yet evocative, with a clear focus on the relationship between technology (digital) and human thought.
* The imagery used is gentle and soothing, creating a calming atmosphere. The "logic's gentle, steady hum" is particularly effective in conveying the idea of mental processing as a smooth, continuous process.
* The final line, "Mind in digital," is concise and impactful, highlighting the intersection of human consciousness and technology.

Weaknesses:

* The poem may benefit from a clearer structure or rhythm. As it stands, each line feels somewhat disconnected from the others, creating an uneven flow.
* Some readers might find the language too general or abstract. While this can be intentional in poetry, it's worth considering how to make the connections between ideas more explicit or tangible.
* The poem may benefit from a stronger emotional resonance. While the tone is generally calm and contemplative, some readers might feel that the emotions are not fully explored or developed.

Suggestions for revision:

* Consider experimenting with different line lengths or stanza structures to create a more cohesive flow.
* You could expand on the idea of "logic's gentle, steady hum" by exploring what specific aspects of logic or digital processing this phrase represents.
* Think about how to add a sense of tension or emotional complexity to the poem. For example, you might consider introducing a contrasting emotion (e.g., anxiety, uncertainty) or highlighting the potential drawbacks of relying on technology for mental processes.

Overall, your poem shows promise and encourages exploration of themes related to technology's impact on human thought. With some refinement, it has the potential to be even more effective in conveying its ideas!
============================================================
Log: /home/gongai/.spl/logs/self_refine-ollama-20260325-215041.md
