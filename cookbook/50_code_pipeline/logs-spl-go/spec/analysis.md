[READY]
The implementation will be a standalone Python function that accepts a string and returns a new string containing the characters of the input in reverse order.

STRUCTURED SPEC:
1. The function must be named `reverse_string` and accept one argument, `s`, of type `str`.
2. The function must return a value of type `str`.
3. The function must return a new string where the characters are in the reverse order of the input string `s`.
4. If the input string `s` is empty (`""`), the function must return an empty string (`""`).
5. All characters within the string must be treated as individual units for reversal (i.e., character reversal, not byte reversal).